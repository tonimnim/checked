# ChessKenya (Checked) Environment Configuration
# ================================================
# For Railway deployment: Set these as environment variables in the dashboard
# The database will automatically use /data/chesskenya.db when volume is mounted

# App settings
APP_NAME=ChessKenya
ENVIRONMENT=production
DEBUG=false

# Database
# Local dev: sqlite+aiosqlite:///./chesskenya.db
# Railway (auto-detected): sqlite+aiosqlite:///data/chesskenya.db
# DATABASE_URL is auto-configured, only set if you need to override

# JWT Authentication
SECRET_KEY=your-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days

# Chess.com API
CHESS_COM_API_BASE=https://api.chess.com/pub

# Africa's Talking SMS (for OTP)
# Get credentials at: https://account.africastalking.com
AT_USERNAME=sandbox
AT_API_KEY=your-sandbox-api-key
AT_SENDER_ID=  # Optional: Your registered sender ID (production only)

# Web Push Notifications (VAPID)
# Generate keys: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT_EMAIL=admin@chesskenya.com

# M-Pesa (coming soon)
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_SHORTCODE=
MPESA_PASSKEY=
MPESA_CALLBACK_URL=
MPESA_ENVIRONMENT=sandbox
